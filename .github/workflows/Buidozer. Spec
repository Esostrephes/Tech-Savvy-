[app]
title = MyApp
package.name = myapp
package.domain = org.example
source.dir = .
source.include_exts = py, kv, png, jpg, jpeg, json, txt, mp3, mp4, wav
version = 1.0
requirements = python3, kivy
orientation = portrait
fullscreen = 0

# If your main file is not main.py, change it here
entrypoint = main.py

# Permissions (add more later if needed)
android.permissions = INTERNET, WRITE_EXTERNAL_STORAGE, READ_EXTERNAL_STORAGE

[buildozer]
log_level = 2
warn_on_root = 1

[app/android]
# Uncomment if you want a custom icon later
# icon.filename = %(source.dir)s/icon.png
